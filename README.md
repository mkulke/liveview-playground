# LiveView.js Exploration

## Build

```bash
npm ci
npm run build
npm run client-build
npm run copy-views
```

## Run

(views should be available in `/light` & `/map`)

```bash
node dist/index.js
```
